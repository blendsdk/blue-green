# =============================================================================
# {{PROJECT_NAME}} — Git Ignore Rules
# =============================================================================

# --- Environment & Secrets ---
# Never commit actual environment files or secrets
.env
local_data/
# infra-secrets.env holds real host addresses / SSH key paths — never commit it.
# (It lives under local_data/ which is already ignored; listed here for clarity.)
local_data/infra-secrets.env
# Deploy key generated by `ssh-keygen ... -f deploy_key` (private key — never commit).
deploy_key
deploy_key.pub


# --- Node.js ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Build artifacts ---
dist/
build/
*.tgz

# --- Docker data volumes ---
data/postgresql/
data/redis/

# --- OS files ---
.DS_Store
Thumbs.db

# --- IDE ---
.idea/
.vscode/
*.swp
*.swo

# --- Deployment artifacts ---
# Tarballs created by deploy-package.sh
deployment-package.tar.gz
